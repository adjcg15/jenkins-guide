version: "3.8"

services:
  todolist:
    image: tristanstgo/todolist-app
    container_name: todolist-container
    ports:
      - "8081:8081"
    volumes:
      - ./data:/data
    restart: unless-stopped
